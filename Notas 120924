-------------------Parseo-------------------

Ismael

Ver con chaty como se debe gestionar los errores en el parseo del archivo de configuarción 
    ¿cuáles de ellos paran el compilado?
    ¿cuales generar página de error?

Ver los fd del CGI, hacerlos no bloqueantes y luego añadirlos a la estructura de epoll y comprobar 
cómo se ven afectados por EPOLLIN | EPOLLOUT
Posibilidad de trackearlo con un vector de fd de lectura/escritura
Variables de entorno añadirlas
Investigar qué mierdas se hace con las peticiones POST y DELETE
Sobre todo DELETE, no tiene mucho sentido pero verlo por si acaso.
Ver headers para Alfonso y ver cuáles implementar (no te compliques, usa GPT)
Ver si en los headers se puede establecer el valor de la uri máxima.

	// error_page 400						/error/400.html;
  	// error_page 404 405				 	/error/40x.html;
    // error_page 50x 						/error/50x.html;

Investigar que ocurre si el error es 403 y no hay plantillas de error en el archivo de config?


Investigar si podemos seguir usando istream parar construir el body y el response


Alfonso

Ver el index del archivo de configuración - Ismael mira por encima y añade en el location
Ver el index del archivo de configuración - Alfonso debe comparar el index para añadir al PATH en el target
std::vector<std::string> index;
Ver plantillas para errores

Redirecciones ver codigo de ismael para consultar y fijar targets




Interesante tener en cuenta la hoja de eval