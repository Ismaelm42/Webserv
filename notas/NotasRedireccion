
Si hay redirección:

Alamacenar en "_location" la nueva dirección
Establecer el código de estatus(error) 
devolver maroy que 0 



Esto provoce que salgamos de la función getTarget para buscar el target con valor 
"positivo"

Esto a su vez devuelve uno desde la función build Body

Que provoca que se llame a la función buldErrorBody

Esta función chequea si hay alguna página para el _code almacenado y si 
pero para las redirecciones parece que solo sería necesario almacenar el codigo por si al leer el archivo diera otro error

